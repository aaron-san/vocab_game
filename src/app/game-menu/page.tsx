import Word from "@/components/Word";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <>
      <div>Game Menu</div>
      <Word />
    </>
  );
};

export default page;
